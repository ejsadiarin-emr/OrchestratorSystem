major rehaul

- "Workloads"
    - predefined specific instructions or predefined initialization steps
    - consists of multiple packages (2-3 packages for each workload for this PoC)
        - HAS concrete packages defined already
    - has install (first-time install) and update (from an existing install) modes
        - manage "lifecycle" of workloads
        - example for updating a workload:
            - Given workload 1.0: has PKG1,PKG2,PKG3 -> but has update on PKG3.
                - what are the steps to take to update PKG3 carefully AND THEN update version of workload 1.0 to workload 1.1?
                - ex. discord has chat, voice, channel modules (like "packages") - if voice module has an update then how to make sure it updates while not risking other modules (most likely it won't be affected though)
    - both exists in orchestrator and agent - note that we can install "workloads" on the orchestrator node as well (not just in agent nodes)
    - raw meeting notes for "workloads" discussion:
        - workload - predefined pkgs beforehand
            - install/update modes - predefined initialization steps
            - ex. poc - 2-3 pkgs in one workload

- [Artifact Store] assume there are already packages in artifact store (defined and stored)
    - how then we update packages? - cascades to "workloads" and version bumps (see example above in workload section)

- [OTEL]: file-based is OK but for querying the logs?
    - look for better alternatives to file-based - maybe do OTel collector + exporter/store in prometheus (is this lightweight enough? - or is clickhouse appropriate for this? - im leaning more towards prometheus though)
    - SHOULD HAVE a Log Viewer - can be an external app or service (recommend me here)
        - if none, then custom log viewer - but this is worst case scenario
    - FOR Orchestrator: still needs a way to monitor the Workloads mentioned (so like minimal log viewer or status monitor - see which packages are being installed/updated - see which nodes are having work, etc.) so then how do you stream agent logs? - here no need to overcomplicate.

- bare minimum fields to make it work (for system admin UX)
    - [API request schema] (THIS IS GOOD) but separate out required/optional - make it so that system admins only NEED to fill in a few fields (the most minimal to make it work).
        - `manifest.packageId`
        - `manifest.displayName`
        - `manifest.version`
        - `manifest.channel` (`stable|canary|test`)
        - `manifest.artifactType`
        - `manifest.installAdapter.type`
        - `manifest.installAdapter.command`
        - `manifest.installAdapter.arguments`
        - `manifest.installAdapter.expectedExitCodes`
        - `manifest.installAdapter.timeoutSeconds`
        - `manifest.detection.type`
        - `manifest.detection.path`
        - `manifest.detection.expectedVersion`
        - `manifest.originMetadata.source`
        - `manifest.originMetadata.publisher`
        - `manifest.policyTags.retryabilityClass`
        - `manifest.policyTags.idempotencyMode`
        - `manifest.policyTags.riskLevel`
        - `manifest.policyTags.approvalRequired`
    - [Stored manifest record (post-ingest)] create JSON schema validator:
        - note that if this can be trimmed then trim, IF NOT then don't trim at all (if all fields here below in the schema are important)
        ```json
        {
            "artifact": {
                "source": "/api/artifacts/nodejs/24.0.0",
                "type": "zip",
                "sizeBytes": 34567890,
                "digest": {
                    "algorithm": "sha256",
                    "value": "<immutable-content-hash>"
                },
                "signature": {
                    "type": "authenticode-or-detached",
                    "publisher": "CN=VendorOrInternalSigning",
                    "verification": "pass|warn|fail"
                }
            },
            "originMetadata": {
                "source": "vendor-repo-or-internal-mirror",
                "publisher": "vendor-if-known",
                "ingestedBy": "operator-or-process-id",
                "ingestedAtUtc": "timestamp",
                "verificationResult": "pass|warn|fail"
            },
            "policyTags": {
                "retryabilityClass": "transient_only",
                "idempotencyMode": "version_check",
                "riskLevel": "medium",
                "approvalRequired": false
            }
        }
        ```

- UPDATE on Orchestrator and Agents - FOCUS on UX (everything should be web-based - but still self-contained) so BOTH have Web UI (React) + BG Service (backend of course):
    - orch - bg svc + web ui + some other components if needed (sqlite, otel collector,otel store, artifact store, etc.)
        - orchestrator can also have "workloads" installed
    - agent - bg svc + web ui
        - agents have "workloads" installed
        - so like users of "agents" are the customers - make them have easy UX for updating agent's workloads.

- Advised to create UI mockups to validate UI/UX flow - should make it easier for agents to update their workloads (minimum fields to configure and/or "one-tap" button in agent's web UI ?) - same as in orchestrator (since workloads also exists in orchestrator)
