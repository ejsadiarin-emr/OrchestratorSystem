# 2025-05-05

- /workload-runs - have args, parsed to include in workloads
    - templated workload definition JSON?

- EDGE CASE: how to handle "restart" on agent for packages that needs restart to update.

## Goals

- [ ] .http for all endpoints (orchestrator & agent)
- [ ] [UI]: status "Unknown" on "Update" mode (Install) on step 4 pre-checks on "Run creator" modal
    - ex. Amazing workload v1 to v2 -> status shows "Unknown" 
- [ ] [VERIFY]: pre-checks (see DeploymentPoC-detailed.md file)
    - need better pre-checks (inconsistent output - has stale data) - might have inconsistent logic
    - must update DB every pre-check(?)
- [ ] refine preInitSteps & postInitSteps, preUninstallSteps & postUninstallSteps, preWorkloadSteps & postWorkloadSteps
    - run an in-depth review and audit if this functionality is already supported
- [x] [VERIFY]: how agent sends its state to orchestrator
    - does orchestrator hit an endpoint to ask for current agent state?
        - *yes, orchestrator hits /api/detect endpoint then agent returns response:*
        ```json
        {
            "results": [
                {
                    "packageId": "58731731-098c-59f0-af67-790a62ad660e",
                    "name": "dbeaver",
                    "status": "NotPresent",
                    "actualVersion": null,
                    "expectedVersion": null,
                    "comparison": null
                },
                {
                    "packageId": "6c81318e-b3bc-25a5-1770-423b9bbdff22",
                    "name": "python",
                    "status": "AlreadySatisfied",
                    "actualVersion": "3.13.3",
                    "expectedVersion": null,
                    "comparison": null
                }
            ],
            "diskInfo": {
                "freeBytes": 123456789012,
                "totalBytes": 499999999999,
                "drive": "C:\\"
            }
        }
        ```
    - step by step flow visualized
