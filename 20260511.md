# 2026-05-11

## Goals
- [ ] .http for all endpoints (orchestrator & agent)
- [x] [UI]: status "Unknown" on "Update" mode (Install) on step 4 pre-checks on "Run creator" modal
    - ex. Amazing workload v1 to v2 -> status shows "Unknown" 
    - pre-checks "Unknown" (on /workload-runs Run Creator wizard modal - step 4%5) state and "Drifted" (on /nodes)
- [x] [VERIFY]: pre-checks (see DeploymentPoC-detailed.md file)
    - need better pre-checks (inconsistent output - has stale data) - might have inconsistent logic
    - must update DB every pre-check(?)
- [x] refine preInitSteps & postInitSteps, preUninstallSteps & postUninstallSteps, preWorkloadSteps & postWorkloadSteps
    - run an in-depth review and audit if this functionality is already supported

##  Notes

if kailangan ng requirements ung restart, dapat workload level ung pagrerestart ng node.
 - e.g. ung package, pwede siya magsabi kung nagtrigger ng restart required. pero di pa dapat magrestart on a per package installation. sa buong workload sana gagawin. 
 
siguro maglagay nalang ng provision din na need mag hard restart just in case needed siya on the package level
 
may flag na nilalagay ung windows sa registry to flag kung reboot required na global
 
so pwedeng yun ung basis
 
---
Research about this new requirement and the best way to do this in our codebase: "Support packages that require a restart especially when updating" 
- I'm thinking that the restart would happen at the workload-level (post workload) - currently we have PostWorkloadSteps but is there a way on doing this better like a field to indicate that a package requires restarts if updating it (like package-level manifest JSON field called "restartRequired": true|false)?
- It was also mentioned that a flag can be put into the windows registry to flag the "restart/reboot required" globally - can be the basis?

What do you think?

